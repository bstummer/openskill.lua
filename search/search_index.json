{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Introduction","text":"<p>openskill.lua is an implementation of the Weng-Lin Bayesian ranking, a better, license-free alternative to TrueSkill.</p> <p>It is a port of the amazing openskill.js module.</p> <p>Firstly, get the module here and insert it into your game (preferably ServerStorage). You can also just paste this in the command bar: <pre><code>game:GetObjects(\"rbxassetid://8134663273\")[1].Parent=game.ServerStorage\n</code></pre></p> <p></p> <p>Create a script and require the module: <pre><code>local OpenSkill = require(game.ServerStorage.OpenSkill)\n</code></pre></p> <p></p> <p>You can create a rating for every player, which describes their skill. Ratings are kept as an object which represent a gaussian curve, with properties where <code>mu</code> represents the mean, and <code>sigma</code> represents the spread or standard deviation. Create these with: <pre><code>local a1 = OpenSkill.Rating() --&gt; {mu = 25, sigma = 8.333333333333334}\nlocal a2 = OpenSkill.Rating(32.444) --&gt; {mu = 32.444, sigma = 10.81466666666667}\nlocal b1 = OpenSkill.Rating(nil, 2.421) --&gt; {mu = 25, sigma = 2.421}\nlocal b2 = OpenSkill.Rating(25.188, 6.211) --&gt; {mu = 25.188, sigma = 6.211}\n</code></pre></p> <p></p> <p>If <code>a1</code> and <code>a2</code> are on a team and win against a team of <code>b1</code> and <code>b2</code>, you can update their skill ratings like this: <pre><code>OpenSkill.Rate({{a1, a2}, {b1, b2}})\n</code></pre></p> <p></p> <p>When displaying a rating or sorting a list of ratings, you can use <code>Ordinal</code>: <pre><code>OpenSkill.Ordinal(a1) --&gt; 0\n--after updating the rating above:\nOpenSkill.Ordinal(a1) --&gt; 2.3245624871094\n</code></pre></p> <p></p> <p>If your teams are listed in one order but your ranking is in a different order, you can specify a ranks option, such as: <pre><code>local a = OpenSkill.Rating()\nlocal b = OpenSkill.Rating()\nlocal c = OpenSkill.Rating()\nlocal d = OpenSkill.Rating()\n\nOpenSkill.Rate({{a}, {b}, {c}, {d}}, { --4 teams consisting of 1 player\n    rank = {4, 1, 3, 2}\n})\n</code></pre> It is assumed that the lower ranks played better in the game, while higher ranks did worse.  For example, the team of <code>b</code> would be the best of the game.</p> <p></p> <p>You can also provide a score instead, where lower is worse and higher is better. These can just be raw scores from the game, if you want. <pre><code>OpenSkill.Rate({{a}, {b}, {c}, {d}}, {\n    score = {37, 19, 37, 42}\n})\n</code></pre> Ties should have either equivalent rank or score.</p> <p></p> <p>openskill.lua provides two rating models: <code>PlackettLuce</code> and <code>ThurstoneMosteller</code>.</p> <ul> <li>Plackett-Luce is a generalized Bradley-Terry model for k \u2265 3 teams which scales best. It follows a logistic distribution over a player's skill, similar to Glicko.</li> <li>Thurstone-Mosteller rating models follow a gaussian distribution, similar to TrueSkill. Gaussian CDF/PDF functions differ in implementation from system to system (they're all just chebyshev approximations anyway). The accuracy of this model isn't usually as great either, but tuning this with an alternative gamma function can improve the accuracy if you really want to get into it.</li> <li>openskill.lua uses full pairing which should have more accurate ratings over partial pairing, however in high k games (like 100+ teams), Bradley-Terry and Thurston-Mosteller models need to do a calculation of joint probability which involves a k-1 dimensional integration, which is computationally expensive. Partial pairing, where players only change based on their neighbors, is not available yet.</li> </ul> <p>You can change the default model by changing <code>OpenSkill.Settings.DefaultModel</code>. Alternatively you can pass a model option to the <code>Rate</code> function: <pre><code>OpenSkill.Rate({{a}, {b}, {c}, {d}}, {\n    model = \"ThurstoneMosteller\"\n})\n</code></pre> More models are coming in the future.</p> <p></p> <p>Info</p> <p>Contributions are greatly appreciated!</p>"},{"location":"api/","title":"API","text":""},{"location":"api/#settings","title":"Settings","text":""},{"location":"api/#defaultmodel","title":"DefaultModel","text":"<p><pre><code>DefaultModel : string\n</code></pre> Determines the model which is used by default.</p>"},{"location":"api/#functions","title":"Functions","text":""},{"location":"api/#rating","title":"Rating","text":"<p><pre><code>Rating(mu : number?, sigma : number?, options : any?): rating\n</code></pre> Creates a rating object, which describes a player's skill. Ratings are kept as an object which represent a gaussian curve, with properties where <code>mu</code> represents the mean, and <code>sigma</code> represents the spread or standard deviation. <code>mu</code> is the average skill of the player and <code>sigma</code> is the degree of uncertainty in the player's skill. Maintaining an uncertainty allows the system to make big changes to the skill estimates early on but small changes after a series of consistent games has been played.</p>"},{"location":"api/#ordinal","title":"Ordinal","text":"<p><pre><code>Ordinal(rating : rating, options : any?): number\n</code></pre> Represents a player's skill estimate by one number. By default, this returns <code>mu - 3 * sigma</code>, showing a rating for which there's a 99.7% likelihood the player's true rating is higher. So in early games, a player's ordinal rating will usually go up and could go up even if that player loses.</p>"},{"location":"api/#rate","title":"Rate","text":"<p><pre><code>Rate(teams : {{rating}}, options : any?): {{{number}}}\n</code></pre> Updates the values of the ratings and returns the result in arrays.</p>"},{"location":"api/#winprobability","title":"WinProbability","text":"<p><pre><code>WinProbability(teams : {{rating}}, options : any?): {number}\n</code></pre> Calculates the probability of each team winning.</p>"},{"location":"api/#drawprobability","title":"DrawProbability","text":"<p><pre><code>DrawProbability(teams : {{rating}}, options : any?): number\n</code></pre> Calculates the probability of a draw between the teams. This is very useful to determine good team compositions in matchmaking.</p>"}]}